# =============================================================================
# Dépendances de test. Installées en plus de requirements.txt, jamais en
# production : l'image finale ne les embarque pas.
#
#   pip install -r requirements.txt -r requirements-dev.txt
# =============================================================================

pytest==8.4.2
pytest-asyncio==1.2.0
pytest-cov==7.0.0
pytest-xdist==3.8.0

# `httpx` est desormais une dependance de production : il porte le client
# Ollama. Il reste le client de `TestClient` et des appels concurrents.

# Conteneur PostgreSQL éphémère. Le pilote `psycopg` est celui de production :
# tester sur psycopg2 reviendrait à éprouver une autre pile.
testcontainers[postgres]==4.13.0

# Fabriques de données. `faker` est tiré par factory-boy mais épinglé ici :
# les noms générés entrent dans des colonnes contraintes en longueur.
factory-boy==3.3.3
Faker==37.8.0
